Create table V_Last2Hour_From_ReceiptDetail (
        INTERNAL_RECEIPT_LINE_NUM VARCHAR(50) NOT NULL,
        ITEM VARCHAR(50) NOT NULL,
        DATE_TIME_STAMP TIMESTAMP NOT NULL,
        ITEM_STYLE VARCHAR(50) NOT NULL
)